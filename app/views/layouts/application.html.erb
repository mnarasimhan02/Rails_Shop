<!DOCTYPE html>
<html>
<head>
  <title>Bike Shop</title>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/css/bootstrap.min.css">
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.12.2/jquery.min.js"></script>
  <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.5/js/bootstrap.min.js"></script>
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <%= csrf_meta_tags %>
</head>
<body>
<div class="page-wrap">
	<nav class="navbar navbar-inverse">
	  <div class="container-fluid">
	    <div class="navbar-header">
	      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#myNavbar">
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>
	        <span class="icon-bar"></span>                        
	      </button>
	      <a class="navbar-brand" href="#">
	      	<%= image_tag("brandlogo.gif", class: "img-responsive my-image", size:"50x50") %>
	      </a>
	        <%= form_tag("/products", method: "get", class: "navbar-form navbar-left") do %>
  			<%= label_tag(:q, "") %>
  			<%= text_field_tag(:q) %>
  			<%= submit_tag "Search", class: "btn btn-default btn_search"%>
	    </div>
	    <div class="collapse navbar-collapse" id="myNavbar">
	      <ul class="nav navbar-nav navbar-right">
	      	<li>
	      		<% if user_signed_in? %>
   					 <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>
  				<% else %>
    				<%= link_to('Login', new_user_session_path) %>
  				<% end %>
 			</li>
		      <li><a href="/"><span class="glyphicon glyphicon-home"></span> Home</a></li>
		       <li><a href="/static_pages/contact"><span class="glyphicon glyphicon-envelope"></span> Contact us</a></li>
	       </ul>
	    </div>
	  </div>
	</nav>
</div>
<p class="notice"><%= notice %></p>
	<% if notice %>
	    <p id="notice" class="notice notice-info"><%= notice %></p>
	<% end %>
<p class="alert"><%= alert %></p>
	<% if alert %>
	    <p id="alert" class="alert alert-info"><%= alert %></p>
	<% end %>
<% end %>
<%= yield %>
<footer class="site-footer">&copy;2015 Bike Berlin</footer>
</body>
</html>